To compile a file:
javac -sourcepath src/java/ -d target/ src/java/edu/school21/printer/app/Program.java

To compile a jar:
jar -cmf src/manifest.txt <name>.jar target src/resources

To start the program:
java -jar name.jar --black=<sumbol> --white=<symbol>

Checking what is in the jar:

jar tf <name>.jar

Example:

javac -sourcepath src/java/ -d target/ src/java/edu/school21/printer/app/Program.java

jar -cmf src/manifest.txt project.jar target src/resources

java -jar project.jar --black=. --white=0
